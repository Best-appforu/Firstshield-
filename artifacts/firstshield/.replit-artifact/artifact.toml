kind = "web"
previewPath = "/"
title = "FirstShield"
version = "1.0.0"
id = "artifacts/firstshield"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21107

[services.development]
run = "pnpm --filter @workspace/firstshield run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/firstshield", "run", "build" ]
publicDir = "artifacts/firstshield/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21107"
BASE_PATH = "/"
